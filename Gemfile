source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "rails", github: "eileencodes/rails", branch: "move-pluck-to-association"

# Specify your gem's dependencies in active_record_has_many_split_through.gemspec
gemspec
